---
name: Button 按钮
route: /Button
---
import { Playground, Props } from 'docz'
import Button from './index.tsx'

## Button

按钮组件

### 属性值
<Props of={Button} />

### 基本用法
<Playground>
  <Button>default</Button>
  <Button type="primary">primary</Button>
  <Button type="dashed">dashed</Button>
</Playground>

### 尺寸
<Playground>
  <Button type="primary" size="large">large</Button>
  <Button type="primary">default</Button>
  <Button type="primary" size="small">small</Button>
</Playground>

### 铺满
<Playground>
  <Button className="custom-class" type="primary" block>block</Button>
</Playground>

### 禁用
<Playground>
  <Button className="custom-class" type="primary" disabled>
    <span>disabled</span>
  </Button>
</Playground>